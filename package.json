{
  "name": "mediflow-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --port 5001"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
